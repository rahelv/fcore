# Supplemental config for RESUMING the pretrained runs (project
# fcore_best_pretrained) to 250 epochs. Their original configs are restored
# on resume; this file overrides the run-control keys (allow_val_change).
#
#   python3 train.py --config sweep_configurations/resume_250_pretrained.yaml \
#       --resume /home/ubuntu/data/models/fcore_best_pretrained/<run_id>_best.pt

project: fcore_best_pretrained
entity: rahelvalerie-universit-t-basel

save_dir: /home/ubuntu/data/models/fcore_final_250
data_dir: /home/ubuntu/data/robust_dataset_split_30c_v2

max_epochs: 250
min_epochs: 250   # = max_epochs -> early stopping disabled
patience: 250
